services:
  api:
    container_name: pan-webclient-backend
    image: pan-webclient-backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - path: .env
        format: raw
    environment:
      API_PORT: ${API_PORT:-8080}
    volumes:
      - ./configs:/app/configs:ro
      - ./data:/app/data
    expose:
      - "${API_PORT:-8080}"

  frontend:
    container_name: pan-webclient-frontend
    image: pan-webclient-frontend
    build:
      context: .
      dockerfile: frontend/Dockerfile
    depends_on:
      - api
    ports:
      - "${NGINX_PORT:-11946}:80"
